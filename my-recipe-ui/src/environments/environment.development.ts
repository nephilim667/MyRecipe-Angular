export const environment = {
    production: false,
    useMocks: true,                                     // Tells my app to intercept and return mock data
    apiBaseUrl: 'https://api.mock-gateway.local/api'    // Dummy URL
};
