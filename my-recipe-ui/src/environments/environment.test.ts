export const environment = {
    production: false,
    useMocks: false,                            // Tells my app to not intercept
    apiBaseUrl: 'https://localhost:7123/api'    // Localhost test API URL
};
